# KevLumos クラウド公開版
運用者向けマニュアル（repo版）

---

## 1. 本書について

### 1.1 本書の目的
本書は、KevLumos クラウド公開版を
**運用担当者が安全かつ継続的に運用するために必要な判断観点と操作指針**
を提供することを目的とする。

本書は実装手順書ではなく、運用フェーズ専用のマニュアルである。

---

### 1.2 本書の対象読者
- KevLumos クラウド公開版の運用を担当する者
- AWS 環境および onprem 環境への操作権限を持つ運用担当者

---

### 1.3 本書のスコープ
本書は以下を扱う。

- 日常運用における状態確認
- 一次対応としてのトラブルシューティング
- 利用者ユーザー管理に関する運用判断および運用上許容される操作

以下は本書の対象外とする。

- 再構築・再デプロイ手順
- 設計変更・構成変更
- upstream 更新追従
- CI / 自動化 / 運用改善提案

---

### 1.4 運用ドキュメントとしての記載方針
運用に類する操作が必要な項目については、
**本文に操作手順は示さず**、
Appendix または別途整備されたドキュメントを参照することを示す。

本文は、運用判断の観点、切り分けの順序、操作の許容範囲を記録する。

---

### 1.5 本書の構成
- 第2章：システム全体の運用視点での構成概要
- 第3章：ユーザー管理（Cognito 運用）
- 第4章：監視・状態確認
- 第5章：トラブルシューティング（一次対応）
- 第6章：Secret / 機密情報の運用と配布モデル（repo版専用）
- Appendix A：運用時の具体操作メモ

---

### 1.6 repo版と配布版について
本書は **repo版**として管理される。

repo版は Git リポジトリ上に配置され、
secret 実値を含まない形で成立する。

運用現場で使用する **配布版**は、
repo版を元に secret 実値を反映し、
不要な章を削除したものとする。

配布版の作成方法については第6章を参照する。
なお、第6章は repo版にのみ存在する。

---

### 1.7 他ドキュメントとの関係
- 利用者向け操作説明：B-08
- 再構築手順書：D-01

---

## 2. システム全体の運用視点での構成概要

### 2.1 運用視点での全体像
KevLumos クラウド公開版は、以下の要素で構成される。

- Cloud frontend（CloudFront + S3）
- 認証基盤（Cognito）
- API 中継（Lambda Function URL / kcs-edge）
- onprem backend（VPN 経由）

運用者は、
「どの層で問題が起きているか」を切り分けられれば十分であり、
各要素の詳細実装を理解する必要はない。

---

### 2.2 運用者が直接触る可能性のあるコンポーネント
- Cognito（利用者ユーザー管理）
- CloudWatch（Lambda 実行状況確認）
- CloudFront / S3（配信・状態確認）
- S3 / SQS（アップロード処理の滞留確認）
- onprem backend（ログ・ディスク使用量確認）

設定変更や再構築は、本書の範囲外とする。

---

### 2.3 運用時の基本スタンス
- 日常運用では「壊れていないか」より「不健全になっていないか」を見る
- 異常を感じた場合は、直ちに変更せず切り分けを行う
- 判断が必要な場合は、関連 Issue に切り出す
- secret 実値を docs / Issue / repo に貼り付けない

---

## 3. ユーザー管理（Cognito 運用）

### 3.1 本章の位置づけ
本章は、KevLumos クラウド公開版における
**利用者ユーザー管理に関する運用判断の範囲**
と、運用担当者が実施してよい操作範囲を定義する。

---

### 3.2 運用担当者による Cognito 利用の前提
- 利用者は Amazon Cognito User Pool 上のユーザーとして管理される
- 運用担当者は、利用者ユーザーの **追加・無効化・状態確認** を行う
- 利用停止は削除ではなく無効化により行う
- 認証方式や App client 構成は変更しない

---

### 3.3 利用者ユーザーの追加
新規利用者の追加が必要な場合、
運用担当者は Cognito User Pool に対してユーザー追加を行う。

- 追加後、ユーザーが想定どおりの状態で作成されていることを確認する
- 追加に失敗する、または想定外の状態になる場合は、運用判断として作業を止め、Issue に切り出す

具体的な操作手順は **Appendix A.1** を参照する。

---

### 3.4 利用者ユーザーの無効化
利用停止が必要な場合、
運用担当者は対象ユーザーを無効化する。

- 原則として削除は行わない
- 無効化後、対象ユーザーが無効化状態であることを確認する

具体的な操作手順は **Appendix A.1** を参照する。

---

### 3.5 利用者ユーザーの状態確認
認証トラブルが疑われる場合、以下を確認する。

- ユーザーが有効（Enabled）であるか
- 状態が CONFIRMED であるか
- 初回ログイン未完了等の途中状態で滞留していないか

具体的な確認手順は **Appendix A.1** を参照する。

---

### 3.6 クライアント設定に起因する問題の確認
ログイン後の遷移や API 利用に失敗する場合、
Client ID や redirect URI の不整合を確認する。

具体的な確認手順は **Appendix A.1** を参照する。

---

### 3.7 本章のまとめ
- 運用担当者は利用者ユーザーの追加・無効化・状態確認を行う
- 認証方式や App client 構成の変更は本章の対象外とする

---

## 4. 監視・状態確認

### 4.1 本章の位置づけ
本章は、KevLumos クラウド公開版における
**日常運用フェーズでの監視および状態確認の観点**
を示す。

本章の目的は、異常の発生を検知することではなく、
システムが不健全な状態に移行していないかを把握し、
一次切り分けにつなげる判断材料を提供することにある。

本章では、確認すべき観点を示し、
具体的な操作方法や画面遷移は Appendix に集約する。

---

### 4.2 Lambda（kcs-edge）の実行状況
API 中継を担う Lambda（kcs-edge）は、
クラウド側における中核的なコンポーネントである。

運用担当者は、以下の観点で実行状況を確認する。

- 実行回数が通常時と比べて極端に増減していないか
- エラーが一時的ではなく、継続的に発生していないか
- 単発の失敗ではなく、連続した失敗として観測されていないか
- Duration が通常時より明らかに長い状態が継続していないか

具体的な確認手順は **Appendix A.3** を参照する。

---

### 4.3 CloudFront によるフロントエンド配信状況
CloudFront は、KevLumos フロントエンド配信を担う。

運用担当者は、以下の観点で配信状況を確認する。

- フロントエンドへのリクエストが継続的に発生していること
- Requests が継続的に 0 でないこと
- 5xxErrorRate が高止まりしていないこと
- 配信エラーが異常なエラーレートとして継続していないこと

具体的な確認手順は **Appendix A.2** を参照する。

---

### 4.4 アップロード処理関連コンポーネントの状態
アップロード処理では、
S3 の中間バケットや SQS 等が関与する。

運用担当者は、以下の観点で状態を確認する。

- 中間バケットに意図せずファイルが残留していないか
- ファイル数や総容量が継続的に増加していないか
- `uploads/` 配下に明らかに古いファイルが滞留していないか
- SQS にメッセージが継続的に滞留していないか
- 滞留が発生している場合、その発生源が想定できるか
    - 処理失敗
    - 通知不備
    - 後続処理停止 など

具体的な確認手順は **Appendix A.4** を参照する。

---

### 4.5 onprem backend の状態
onprem backend では、
処理対象データや一時ファイルが
`/srv/kevlumos/uploads` 配下に保存される。

運用担当者は、以下の観点で状態を確認する。

- ログ上で同一エラーが短時間に連続していないか
- ディスク使用量が通常時と比べて異常に増加していないか
- 明らかに古い一時ファイルが残留していないか
- 短時間での急激な増加が発生していないか

具体的な確認手順は **Appendix A.5** を参照する。

---

### 4.6 本章のまとめ
- 本章は日常運用における状態確認の判断観点を示す
- 異常兆候を把握し、一次切り分けにつなげることを目的とする
- 操作手順は Appendix に集約する

---

## 5. トラブルシューティング（一次対応）

### 5.1 本章の位置づけ
本章は、KevLumos クラウド公開版において
障害や異常が疑われる場合の
**一次対応としての切り分け観点**を示す。

本章の目的は、
問題の発生箇所を特定し、
次の対応判断につなげることである。

本章では「どこに問題がありそうか」を判断するための視点を示し、
具体的な操作手順や画面遷移は Appendix に集約する。

---

### 5.2 切り分けの基本方針
トラブル発生時は、以下の観点で切り分けを行う。

- frontend 側の問題か
- cloud 側（CloudFront / Lambda / S3 / SQS 等）の問題か
- onprem backend 側の問題か
- 一時的な問題か、継続的な問題か

---

### 5.3 フロントエンド起点の問題
フロントエンド起点の問題が疑われる場合、
以下の観点で確認を行う。

- CloudFront の配信が正常に行われているか
- フロントエンドのリソース取得に失敗していないか
- ログイン後の API 呼び出しで失敗していないか

具体的な確認手順は **Appendix A.2** を参照する。

---

### 5.4 API / アップロード処理に関する問題
API 呼び出しが失敗する、またはアップロード処理が途中で失敗する場合、
以下の観点で切り分けを行う。

- Lambda（kcs-edge）でエラーが発生していないか
- 呼び出し回数や Errors が継続的に異常でないか
- 中間バケットにファイル残留が発生していないか
- SQS 通知が滞留していないか
- upload 完了処理の滞留が疑われないか

具体的な確認手順は **Appendix A.3 / A.4** を参照する。

---

### 5.5 認証・認可に関する問題
ログインできない、または認証後の操作に失敗する場合、
以下の観点で確認を行う。

- Cognito ユーザーが無効化されていないか
- ユーザー状態が途中状態で滞留していないか
- クライアント ID の混同がないか
- redirect URI が想定どおりであるか

具体的な確認手順は **Appendix A.1** を参照する。

---

### 5.6 cloud / onprem 間通信に関する問題
cloud 側の処理が onprem backend に到達しない、
または応答が返らない場合、
以下の観点で確認を行う。

- Lambda から onprem backend への通信が失敗していないか
- backend 側で受信ログが確認できるか
- internal header により backend 側で拒否されていないか
- 通信失敗が一時的か、継続的か
- 同一エラーが 15 分以上継続していないか

具体的な確認手順は **Appendix A.6** を参照する。

---

### 5.7 本章のまとめ
- 本章は一次対応としての切り分け観点を示す
- 問題の発生箇所を段階的に特定することを目的とする
- 具体的な操作は Appendix に委ねる

---

## 6. Secret / 機密情報の運用と配布モデル

### 6.1 本章の位置づけ
本章は、KevLumos クラウド公開版の運用において使用される
secret / 機密情報の取り扱い方針と、
本書の **配布モデル（repo版 / 配布版）** を定義する章である。

本章は **repo版にのみ存在する**。
運用者向けに配布される配布版には、本章は含まれない。

---

### 6.2 repo版と配布版の存在理由
KevLumos クラウド公開版の運用では、
以下の二つの要件を同時に満たす必要がある。

- Git リポジトリ上で管理可能であること
- 運用者が即時に作業できること

これを両立するため、本書は以下の二系統で運用される。

#### repo版（正本）
- Git リポジトリで管理される
- secret / 機密情報の実値を含まない
- 論理識別子・判断基準・運用構造を記載する
- 本書の **唯一の正本** である

#### 配布版（運用者向け）
- repo版をもとに作成される派生物
- 運用に必要な secret 実値を反映する
- 現場での即時性を優先する
- 正本ではない

---

### 6.3 Secret 情報の原本と管理原則
KevLumos クラウド公開版で使用される
secret / 機密情報の **原本（一次情報）** は、
KevLumos 運用担当が管理する **非公開の管理資料** に集約される。

この原本は以下の性質を持つ。

- Git リポジトリには含めない
- repo版および Issue には記載しない
- secret の更新・失効・ローテーションは、
  必ずこの原本を起点として行う

repo版では、secret は **論理識別子** としてのみ記載される。

---

### 6.4 配布版の作成方法
配布版は、repo版をもとに以下の手順で作成する。

1. repo版の本文をコピーする

2. 第1章に記載された repo版向け文言を、
   配布版向け文言に置換する

   **repo版文言（第1章 1.6 項 全文）**

   本書は **repo版**として管理される。

   repo版は Git リポジトリ上に配置され、
   secret 実値を含まない形で成立する。

   運用現場で使用する **配布版**は、
   repo版を元に secret 実値を反映し、
   不要な章を削除したものとする。

   配布版の作成方法については第6章を参照する。
   なお、第6章は repo版にのみ存在する。

   **配布版向け文言（置換ブロック）**

   本書は、`docs/ops_cloud.md`（Git リポジトリ管理版）をもとに作成された
   運用者向け配布資料である。

   本書には、運用に必要な設定値および識別子の実値が反映されている。
   内容に変更が必要な場合は、リポジトリ管理版を修正し、
   本書は再生成すること。

3. repo版本文中に記載された論理識別子を、
   secret 原本に基づく実値に置換する

4. 本章（第6章）を配布版から削除する

---

### 6.5 配布版における注意事項
配布版は repo版から生成された派生物であり、
以下の点に留意する。

- 配布版を直接編集しない
- 内容に変更が必要な場合は、repo版を修正する
- repo版修正後、配布版を再生成する
- 配布版を正本として扱わない

repo版と配布版の内容に差異が生じた場合、
repo版を正とする。

---

### 6.6 本章のまとめ
- 本書は repo版と配布版の二系統で運用される
- secret の原本は非公開の管理資料として一元管理される
- repo版は secret 実値を含まない正本である
- 配布版は実値を反映した運用者向け資料である
- 本章は repo版専用章であり、配布版には含めない

---

## Appendix A. 運用時の具体操作メモ

本 Appendix は、KevLumos クラウド公開版の運用において、
運用担当者が **実際に画面やログを確認し、一次判断を行うための具体操作**
を整理したものである。

本文（第3章〜第5章）に記載された判断観点に従い、
該当する Appendix 節を参照して操作を行う。

---

### A.1 認証基盤の操作
（Amazon Cognito）

#### 対象コンポーネント
- Amazon Cognito User Pool

#### 使用するコンソール
- AWS マネジメントコンソール

#### 操作の入り口（利用者ユーザーの追加）
1. AWS マネジメントコンソールにログインする
2. Amazon Cognito を開く
3. User Pools を選択する
4. 対象の User Pool を選択する
5. 「Users and groups」を開く
6. 「Create user」を選択する
7. 必要な属性を入力し、ユーザーを作成する

確認事項：
- 作成後、ユーザーが Enabled 状態であること
- User status が想定どおりであること

---

#### 操作の入り口（利用者ユーザーの無効化）
1. Amazon Cognito の User Pool を開く
2. 「Users and groups」を開く
3. 対象ユーザーを選択する
4. 「Disable」を実行する

判断の目安：
- 利用停止は削除ではなく無効化で行う
- 無効化後、Enabled = No であることを確認する

---

#### 操作の入り口（ユーザー状態確認）
1. AWS マネジメントコンソールにログインする
2. Amazon Cognito を開く
3. User Pools を選択する
4. 対象の User Pool を選択する
5. 「Users and groups」を開く
6. 対象ユーザーを検索する

#### 確認する項目（ユーザー単位）
- Enabled / Disabled
- User status
    - CONFIRMED
    - FORCE_CHANGE_PASSWORD
    - UNCONFIRMED

#### 判断の目安（運用者判断用）
- Enabled = Yes かつ User status = CONFIRMED
    → 正常に利用可能
- Enabled = No
    → 利用停止状態
- FORCE_CHANGE_PASSWORD / UNCONFIRMED
    → 初回ログイン・確認未完了の可能性

---

#### 操作の入り口（クライアント設定確認）
1. Cognito User Pool 画面で「App integration」を開く
2. App client 一覧を確認する
3. 対象 App client を選択する

#### 確認する項目（クライアント）
- Client ID
- Callback URLs（redirect URI）
- Logout URLs

#### 判断の目安
- frontend が使用している Client ID と一致している
- redirect URI が利用中の frontend URL と一致している

不一致がある場合、認証後遷移や API 利用に失敗する可能性が高い。

---

#### 運用上の注意事項（ユーザー管理）
- 利用者ユーザーの削除は行わない
- App client や認証方式の変更は行わない
- 想定外の状態が確認された場合は、運用判断として作業を止め、Issue に切り出す

---

### A.2 Cloud frontend 配信確認
（CloudFront / S3）

#### 対象コンポーネント
- CloudFront
- frontend 配信用 S3 バケット

#### 使用するコンソール / ツール
- AWS マネジメントコンソール
- Web ブラウザ

#### 操作の入り口（CloudFront）
1. AWS マネジメントコンソールにログインする
2. CloudFront を開く
3. Distributions 一覧から対象 Distribution を選択する
4. 「Monitoring」タブを開く

#### 確認するメトリクス
- Requests
- 5xxErrorRate

#### 確認する時間範囲
- 直近 15 分
    必要に応じて 1 時間まで拡張する

#### 判断の目安
- Requests が継続的に 0
    → 配信が行われていない可能性
- 5xxErrorRate が数 % 以上で継続
    → 配信異常の可能性

---

#### 補助確認（ブラウザ）
1. ブラウザで frontend に直接アクセスする
2. DevTools を開く
3. Network で HTML / JavaScript / CSS の取得を確認する
4. Console にエラーが出ていないか確認する

判断の目安：
- 5xx が継続する場合は cloud 側の問題を疑う
- リソース取得が広範に失敗する場合は配信経路問題を疑う

---

### A.3 API 中継・実行状況確認
（Lambda / CloudWatch）

#### 対象コンポーネント
- AWS Lambda（kcs-edge）
- CloudWatch Logs / Metrics

#### 使用するコンソール
- AWS マネジメントコンソール

#### 操作の入り口（メトリクス）
1. AWS Lambda を開く
2. 関数 `kcs-edge` を選択する
3. 「Monitor」タブを開く

#### 確認するメトリクス
- Invocations
- Errors
- Duration

#### 確認する時間範囲
- 直近 15 分
    必要に応じて 1 時間まで拡張する

#### 判断の目安
- Invocations が極端に少ない / 急増している
    → 呼び出し経路異常の可能性
- Errors が連続して発生
    → API 中継処理異常の可能性
- Duration が通常時より明らかに長い状態が継続
    → backend 通信遅延等の可能性

---

#### 操作の入り口（ログ）
1. Lambda 関数画面で「Logs」を開く
2. CloudWatch Logs の該当 Log stream を開く
3. エラーが単発か連続かを確認する

判断の目安：
- 同一エラーが 15 分以上継続している場合は継続障害の可能性が高い

---

### A.4 アップロード処理関連の確認
（S3 / SQS / Lambda）

#### 対象コンポーネント
- S3（ARTIFACTS_BUFFER_BUCKET）
- SQS（upload 完了通知）
- Lambda（kcs-edge）

#### 操作の入り口（S3）
1. S3 を開く
2. ARTIFACTS_BUFFER_BUCKET を選択する
3. `uploads/` 配下を確認する
4. 更新時刻で並べ替え、古いファイルが残っていないか確認する

#### 確認する指標
- ファイル数
- 更新時刻
- 数時間以上前のファイルが継続的に残っていないか

判断の目安：
- 数時間以上前のファイルが継続的に残留
    → 完了処理未実行の可能性

---

#### 操作の入り口（SQS）
1. Amazon SQS を開く
2. 対象キューを選択する
3. 「Monitoring」を開く

#### 確認する指標
- ApproximateNumberOfMessages

#### 確認する時間範囲
- 直近 1 時間
    必要に応じて数時間に拡張し、継続的な滞留かを確認する

判断の目安：
- メッセージが継続的に滞留
    → 後続処理異常の可能性

---

### A.5 onprem backend 状態確認
（VPN 経由）

#### 対象コンポーネント
- onprem backend ホスト
- backend コンテナ
- `/srv/kevlumos/uploads`

#### 使用するツール
- SSH
- docker / docker compose

#### 操作の入り口（ログ）
1. onprem backend ホストにログインする
2. backend コンテナのログを確認する

#### 確認コマンド例
- `docker logs <container>`

判断の目安：
- 同一エラーが短時間に連続出力
    → backend 処理異常の可能性

---

#### 操作の入り口（ディスク使用量）
1. `/srv/kevlumos/uploads` の使用量を確認する
2. 全体ディスク空き容量を確認する

#### 確認コマンド例
- `du -sh /srv/kevlumos/uploads`
- `df -h`

判断の目安：
- ディスク容量が急激に増加
    → 一時ファイル滞留の可能性

---

### A.6 cloud / onprem 間通信の確認
（internal 経路）

#### 対象コンポーネント
- Lambda（kcs-edge）
- onprem backend

#### 操作の入り口
- Lambda：CloudWatch Logs
- backend：コンテナログ

#### 確認観点
- Lambda 送信ログと backend 受信ログの対応
- internal header による拒否の有無
- 通信失敗が一時的か継続的か

#### 確認する時間範囲
- 同一エラーが 15 分以上継続しているかを確認する

#### 判断の目安
- 送信あり・受信なし
    → VPN / 経路問題の可能性
- 拒否ログが継続
    → internal 設定不整合の可能性
